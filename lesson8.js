// undefinedとnull
// nullとは
// 意図的に値が存在しないことを表します（使う時は代入する）

// undefinedとは
// まだ値が代入されていない変数はundefined型となる
console.log(null);
console.log(undefined);

// 変数に何も入れずに宣言したままだとundefinedになる。
// つまりundefinedはJavaScript的にわからないよって言ってる感じ
let x;
console.log(x);

// nullは意図的に値がないことを表す。

